# Research Data Source Boundary

This subtree represents the data/source-contract side of the active research system.

Current source-shaped status:

- `full_market_source_contract.py` is the key retained boundary for source identity, scope compatibility, and source-boundary attachment.
- Downstream label panels, trainable matrices, OOF run specs, and confirmation readiness checks are expected to carry compatible source-boundary evidence.
- Diagnostic or narrow-scope artifacts are explicitly blocked from satisfying broader claims.
- The showcase keeps only contract shape and status; source counts, certificate values, vendor schemas, and paths are omitted.

Reviewer signal:

- data lineage is not an afterthought or a report note
- source compatibility is checked before labels, trainables, and OOF evidence can claim broader scope
- the boundary lives in the source tree where downstream modules can import/validate it, not only in documentation
