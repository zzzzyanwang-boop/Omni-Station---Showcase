# Research OS Applications

This subtree represents Layer 4 application ownership.

Current source-shaped status:

- `contracts.py` describes the application contract surface: WorkOrders enter named applications, applications declare dependencies and output evidence packets, and lower-layer stations remain tools.
- `setup_d_confirmation_readiness.py` is a concrete gate/preflight application. It freezes confirmation scope and validates source refs, panel evidence, OOF run-spec compatibility, native sequence-kernel eligibility, repository-proof inputs, and offline posture before launch references can be produced.

Reviewer signal:

- Layer 4 is not a collection of loose runners
- applications bind governance intent to lower-layer evidence and engine calls
- readiness and launch eligibility are separate from research conclusions and runtime authorization
