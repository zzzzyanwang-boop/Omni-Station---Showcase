# Decision Runtime

Decision runtime resolves scores into action-intent or abstention semantics under explicit contracts.

Reviewable responsibilities:

- score-to-action boundary
- abstention policy
- action validation
- runtime posture separation
