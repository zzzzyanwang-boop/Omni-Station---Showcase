# Engines

Engines perform bounded computation under explicit contracts.

Typical responsibilities:

- validate required inputs
- execute deterministic materialization or evaluation
- avoid hidden fallback behavior
- report schema, hashes, timing, and failure reasons
- return outputs through manifests instead of loose files

