# Evidence DAG

The Evidence DAG records dependency structure between inputs, computations, artifacts, gates, and claims.

Reviewable responsibilities:

- node identity and dependency ordering
- lineage from source to claim
- stale artifact detection
- no-bypass proof for downstream consumers
